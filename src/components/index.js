import xinButton from './button'
import xinInput from './input'

// const components = [
//   xinButton,
//   xinInput
// ]

// const install = function (vue) {
//   console.log('vue: ', 1);
//   components.forEach(component => {
//     vue.component(component.name, component)
//   })
// }

// if (typeof window !== 'undefined' && window.Vue) {
//   console.log('window: ', 2);
//   install(window.Vue);
// }

export default {
  xinButton,
  xinInput,
  // install
}
export {
  xinButton,
  xinInput,
  // install
}